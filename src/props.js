import React from "react";
import Bind from "./Bind";

export default class Props extends React.Component {
  render() {
    return (
        <div>
      <Bind signal= "Red"/>
      </div>
    );
  }
}
